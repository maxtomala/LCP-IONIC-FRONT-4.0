import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TrofeosConseguidosPage } from './trofeos-conseguidos.page';

describe('TrofeosConseguidosPage', () => {
  let component: TrofeosConseguidosPage;
  let fixture: ComponentFixture<TrofeosConseguidosPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(TrofeosConseguidosPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
