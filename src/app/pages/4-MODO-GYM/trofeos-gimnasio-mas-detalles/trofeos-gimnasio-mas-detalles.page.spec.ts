import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TrofeosGimnasioMasDetallesPage } from './trofeos-gimnasio-mas-detalles.page';

describe('TrofeosGimnasioMasDetallesPage', () => {
  let component: TrofeosGimnasioMasDetallesPage;
  let fixture: ComponentFixture<TrofeosGimnasioMasDetallesPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(TrofeosGimnasioMasDetallesPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
