// se ha creado este archivo par aq funcione la libreria de  de cuadro de eliminatoria

// import { merge } from 'webpack-merge';
// import commonConfig from './webpack.common.js'; // Asegúrate de que este archivo existe

// export default merge(commonConfig, {
//   resolve: {
//     fallback: {
//       util: require.resolve('util/'), // Agrega un polyfill para el módulo util
//       stream: require.resolve('stream-browserify'), // Agrega un polyfill para el módulo stream
//     },
//   },
// });

// webpack.config.js
// const path = require('path');

// module.exports = {
//   resolve: {
//     fallback: {
//       "assert": require.resolve("assert/"),
//       "util": require.resolve("util/"),
//       "buffer": require.resolve("buffer/"),
//       // Agrega más polyfills según sea necesario
//     }
//   }
// };
